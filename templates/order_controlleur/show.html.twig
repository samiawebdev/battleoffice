{% extends 'base.html.twig' %}

{% block title %}Order{% endblock %}

{% block body %}
    <h1>Order</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ order.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_order_controlleur_index') }}">back to list</a>

    <a href="{{ path('app_order_controlleur_edit', {'id': order.id}) }}">edit</a>

    {{ include('order_controlleur/_delete_form.html.twig') }}
{% endblock %}
